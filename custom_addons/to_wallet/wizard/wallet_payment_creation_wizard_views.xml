<odoo>

	<record id="wallet_payment_creation_wizard_form_views" model="ir.ui.view">
		<field name="name">wallet.payment.creation.wizard.form</field>
		<field name="model">wallet.payment.creation.wizard</field>
		<field name="arch" type="xml">
			<form>
				<sheet>
					<div role="alert" class="alert alert-info text-center">
						<strong>Note: </strong>
						<span>Enter the reference of the pending wallet transaction history into <strong>Reference</strong> to link it with the new payment.</span>
					</div>
					<field name="currency_id" invisible="1" />
					<field name="company_id" invisible="1" />
					<group>
						<group>
							<field name="payment_date" />
							<field name="partner_id" widget="many2one_avatar" />
							<field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" />
							<field name="confirm_payment" />
							<field name="journal_id" />
						</group>
						<group>
							<field name="type" readonly="1" />
							<field name="wallet_type_id" readonly="1" />
							<field name="amount" />
							<field name="currency_id" groups="base.group_multi_currency" options="{'no_create': True}" />
							<field name="payment_ref" />
						</group>
					</group>
				</sheet>
				<footer>
					<button name="action_process" string="Process" type="object" class="btn-primary" />
					<button string="Cancel" class="btn-secondary" special="cancel" />
				</footer>
			</form>
		</field>
	</record>

</odoo>
