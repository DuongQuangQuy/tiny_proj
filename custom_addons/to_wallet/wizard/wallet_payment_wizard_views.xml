<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_wallet_payment_wizard_form" model="ir.ui.view">
        <field name="name">wallet.payment.wizard.form</field>
        <field name="model">wallet.payment.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="company_id" invisible="1" />
                    <field name="move_line_ids" invisible="1" />
                    <field name="account_type" invisible="1" />
                    <group>
                        <group>
                            <field name="payment_date" />
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" />
                            <field name="journal_id" />
                            <field name="wallet_type_id" />
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="acction_process" string="Process" type="object" class="btn-primary" />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="wallet_payment_wizard_action"
        model="ir.actions.act_window">
        <field name="name">Register Payment By Wallet</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">wallet.payment.wizard</field>
        <field name="binding_model_id" ref="account.model_account_move" />
        <field name="binding_view_types">list</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
